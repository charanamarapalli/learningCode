package api.random;/*
package api;

import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Taking input from the user
        System.out.print("Enter an integer: ");
        int n = scanner.nextInt();  // First line input

        System.out.print("Enter another integer: ");
        int m = scanner.nextInt();  // Second line input

        int[] arr = new int[n];
        System.out.println("Enter " + n + " integers:");
        for (int i = 0; i < n; i++) {
            arr[i] = scanner.nextInt();  // Third line input
        }

        Arrays.sort(arr); // Sort the array in ascending order

        int closestSum = Integer.MAX_VALUE;
        int num1 = 0;
        int num2 = 0;

*/
/*
        int left = 0;
        int right = ;

        // Find the pair of digits with the closest sum to the target number using Two Pointers technique
        while (left < right) {
            int sum = arr[left] + arr[right];
            if (Math.abs(sum - m) < Math.abs(closestSum - m)) {
                closestSum = sum;
                num1 = arr[left];
                num2 = arr[right];
            }*//*


            if (sum < m) {
                left++;
            } else {
                right--;
            }
        }
    }
}













*/
